{
    "description": "Filters the lines based on the ratio between num_src_tokens and num_trg_tokens",
    "command": "./src_trg_ratio.py ${LOG:+--log} --ratio-length $RATIO --min-length $MIN_LENGTH --ratio-type $RATIO_TYPE",
    "parameters":
    {
        "RATIO":
        {
            "type": "float",
            "default": 0.6
        },
        "LOG":
        {
            "type": "bool",
            "default": false,
            "help": "Use log(a)/log(b) to compensate for shorter sentences"
        },
        "MIN_LENGTH":
        {
            "type": "int",
            "default": 10,
            "help": "Ratio will be calculated only for lines with a specified minimum length (calculated by same RATIO_TYPE element)"
        },
        "RATIO_TYPE":
        {
            "type": "str",
            "default": "words",
            "allowed_values":
            [
                "chars",
                "words"
            ],
            "help": "Ratio done by amount of characters or words"
        }
    },
    "type": "bilingual"
}